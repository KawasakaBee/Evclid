document.addEventListener("DOMContentLoaded", function () { document.querySelectorAll(".work__button").forEach(function (e) { e.addEventListener("click", function (e) { const t = e.currentTarget.dataset.path; document.querySelectorAll(".work__image").forEach(function (e) { e.classList.remove("work__image--active") }), document.querySelector(`[data-target="${t}"]`).classList.add("work__image--active") }) }) }); const lazyLoadInstance = new LazyLoad({}); lazyLoadInstance();
